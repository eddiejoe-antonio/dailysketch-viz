console.log("test");

// Create a function that adds each image dynamically within a responsive grid
function createImageGrid(){
    var photo = document.createElement('img');
    photo.src= [`${imageCounter}`].img;
}